{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm --prefix port ci",
  "buildCommand": "npm --prefix port run build",
  "outputDirectory": "port/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}